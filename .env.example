# App
PORT=8000
HISTORICO_FILE=/data/historico.json

# Public host for Caddy.
# Use a domain for automatic HTTPS, for example: licitapro.seudominio.com.br
# Use :80 to serve plain HTTP while testing by IP.
APP_HOST=:80

# AI providers
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GROQ_API_KEY=
GROQ_API_KEY2=

# Optional: Supabase/PostgreSQL. Leave empty to use the local JSON file.
DATABASE_URL=
